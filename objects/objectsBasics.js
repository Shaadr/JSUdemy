let myBook = {
    title: "1984",
    author:"George Orwell",
    pageCount: 326
}

console.log(`${myBook.title} by ${myBook.author}`)

myBook.title = 'Animal Farm'

console.log(`${myBook.title} by ${myBook.author}`)

//Challenge area

let person = {
    name: "Mike",
    age: 29,
    location: 'here'
}

console.log(`${person.name} is ${person.age} and lives ${person.location}`)

person.age += 1;

console.log(`${person.name} is ${person.age} and lives ${person.location}`)


