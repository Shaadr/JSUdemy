// 1. You can't define a variable more than once
let petName = "Luna";

petName = "spot";

console.log(petName);

// 2. There are rules related to variable names
// variables must start w/ a letter or two special characters $$ and __
// only $ and _ are valid symbols in variable naming

// 3. Variable names cannot be reserved keywords
// i.e. let  i.e  let let = 12;